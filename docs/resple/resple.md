# resple Setup
https://github.com/ASIG-X/RESPLE/tree/main
* Make resple worksapace 
```bash
cd ~/shared_volume && mkdir resple_ws
```
* Clone resple repo inside the `~/shared_volume/resple_ws/src` **inside the contianer**

```bash

```