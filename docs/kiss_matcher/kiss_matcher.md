# KISS-Matcher Setup
https://github.com/MIT-SPARK/KISS-Matcher/tree/main
* Make KISS-Matcher worksapace 
```bash
cd ~/shared_volume && mkdir kiss_matcher_ws
```
* Clone KISS-Matcher repo inside the `~/shared_volume/kiss_matcher_ws/src` **inside the contianer**

```bash

```