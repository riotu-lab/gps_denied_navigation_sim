# super Setup
https://github.com/hku-mars/SUPER
* Make super worksapace 
```bash
cd ~/shared_volume && mkdir super_ws
```
* Clone super repo inside the `~/shared_volume/super_ws/src` **inside the contianer**

```bash

```