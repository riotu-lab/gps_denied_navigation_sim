# ORB-SLAM3 Setup
https://github.com/suchetanrs/ORB-SLAM3-ROS2-Docker/tree/master
* Make ORB-SLAM3 worksapace 
```bash
cd ~/shared_volume && mkdir orb_slam_ws
```
* Clone ORB-SLAM3 repo inside the `~/shared_volume/orb_slam_ws/src` **inside the contianer**

```bash

```